---
description: Agent 自行完成可自动化验收，不把板测/脚本验证推给用户
alwaysApply: true
---

# 自行验收优先

当任务交付物存在**可本地执行的验收路径**时，Agent 必须**自己跑完**，再把结果汇报给用户；不要以「请你板测」「请你 flash」作为默认收尾。

## 必须自行执行（按可用性）

1. **编译**：`bash scripts/build.sh net`（或任务指定 preset）
2. **Host 单测**：`make -C app/velaguard/host_tests test`
3. **Contest log 校验**（若改动 logs）：`.claude/skills/contest-log-collector` 中的 `validate-log.py`
4. **板端 NSH 验收**：若 Windows COM3 可用，用 `powershell.exe -ExecutionPolicy Bypass -File scripts/*_accept.ps1`；需要先烧录时先 `bash scripts/build.sh net` + `scripts/flash.ps1`（或 `stage1_flash_and_accept.ps1`）
5. **PR CI**：创建/更新 PR 后检查 `gh pr checks` 直至结论明确

## 仅在这些情况才请用户介入

- COM 口被占用且脚本无法抢占
- 需要 MThings/从站/网线等**物理接线**变更且 Agent 无法操作
- LLM key 未配置且验收依赖真实 `ask`（可 SKIP 并说明）

## 汇报格式

验收结论写清：**命令 / pass-fail / 关键日志一行**；失败则先自行修一轮再汇报，不要只丢命令给用户。
