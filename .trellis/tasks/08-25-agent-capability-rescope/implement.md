# 实施计划：手册改写与产品／技术解耦

> 本任务只改文档，不写产品代码。依据 `prd.md` 的 D1–D6 与 `design.md`。

## 执行顺序原则

**先搬后改。** 先把 §16 与 §9 原样迁入新文档并核对无损，再动手改写手册。若边改边搬，迁移和改写的 diff 会纠缠在一起，无法验证「一条未丢」。

## 风险点

- `VelaGuard_项目手册.md`（858 行）与 `VelaGuard_推进方案.md`（371 行）是产品需求的唯一权威来源，改写横跨大部分章节，**改前确认 git 工作区干净**
- §16 是「已确认架构决策」，内容一条都不能丢，只换存放位置
- **§16.6 是混合章节**：Modbus 状态机与阈值/突变判定公式属技术，但「允许多个 active alarm」「首页显示最高优先级」「`acknowledge` 不等于 `resolved`」属产品语义，**必须留在手册**
- `docs/adr/0002` 与 `0005` 描述已被 v2 推翻的云端 Bridge 架构，本次**不修订**（既有问题，另开任务）

---

## 第一步：建立架构决策文档并迁移

新建 `docs/velaguard-architecture-decisions.md`，头部注明：脱胎于手册 §16，是实现契约，与产品手册分工——手册答「做什么/为什么」，本文答「按什么约束实现」。

原样迁入：

| 来源 | 内容 | 注意 |
|---|---|---|
| §16.1 | 设备身份与 MQTT 鉴权（HMAC token 公式、轮换、denylist） | 全迁 |
| §16.2 | MQTT 权限与 QoS 表、会话策略 | 全迁；`docs/velaguard-mqtt-contract.md` 引用需改 |
| §16.3 | ID、时间戳与幂等（`req_id` / `event_id` / `alarm_id`、时间模型） | 全迁 |
| §16.4 | LLM 请求约束 | 全迁；顺带删除 `llm_proxy` realloc / AXI 堆那条实现细节 |
| §16.5 | 网络、ESP-01 与启动顺序 | 全迁 |
| §16.6 | Modbus 状态机、阈值与突变判定公式 | **部分迁**，产品语义留在手册 |
| §16.7 | 日志系统（文件、事件模型、appender、Agent 审计） | 全迁 |
| §16.8 | 配置双槽提交、schema 版本、UI 权限分级 | 全迁 |
| §16.9 | OTA 约束 | 全迁 |
| §16.10 | 构建模式 test / production | 全迁 |
| §9 | 存储布局路径树 | 全迁 |

迁完先核对一遍「十节 + 存储布局」齐全，再进入第二步。

---

## 第二步：改写 `VelaGuard_项目手册.md`

按章节顺序执行，场景改写与解耦一次到位。

| # | 章节 | 动作 |
|---|---|---|
| 1 | 头部 | 标 v3；写入防漂移判定原则（`prd.md` D6）；注明架构决策已移出 |
| 2 | §1.1 | 采用 `prd.md` D5 的定位表述 |
| 3 | §1.2 | 第三类问题（通信时好时坏）归属改为规则库；突出「寄存器读出来是什么意思」 |
| 4 | §1.4 | 「定位通信故障」行的 VelaGuard 列改为「确定性规则库分类」；**新增「识别寄存器语义」行** |
| 5 | §1.5 | 视 SAI 探针结果决定是否加「多媒体」行；暂标待定 |
| 6 | §2.1 | 架构图加上位机分支（标注「配置期」）；USB CDC 定性改为「配置期正式通道 + 救援通道」 |
| 7 | §2.2 | 「Agent 的诊断推理」改为「Agent 的语义标注」；断网降级口径**简化**（规则库不依赖网络） |
| 8 | §2.3 | 沙箱硬约束原样保留；工具清单同步 `design.md` §2.3；「诊断模式」改为「语义标注会话」；删 `tool_guard.c` 提法 |
| 9 | §2.4 | 语音行改为「重估中，取决于 SAI 探针」并**删除错误的 WM8994 论据**；本地音频告警行同样更正；「屏上编辑配置」行补上位机作为承接落点 |
| 10 | §3.1 | **留结论去细节**：保留「片内 Flash 很小、固件跑在片外 QSPI、外部内存 8 MB、eMMC 8 GB」及「运行期不能擦写 QSPI」这条因果；删地址、Bank 号、堆分区表 |
| 11 | §3.2 | 按 F2/F3 更正 WM8994（树内有 codec 驱动，缺 H7 SAI 驱动与板级接线）；补板载 PDM MEMS 麦、line in/out、喇叭排针；删 Kconfig 名与文件路径 |
| 12 | §3.3 当前软件基线 | **整节删除**（defconfig 名、Kconfig 状态、固件体积均属实现现状，代码即真相） |
| 13 | §4.1 分层 | 代码模块名清单改为**产品能力分组**，不点名任何源文件或模块标识符 |
| 14 | §4.2 分工表 | 整表替换为 `design.md` §1 |
| 15 | §4.3 | 保留；补一句其产出是 Agent 语义标注的输入 |
| 16 | §5.1 | 保留；末尾衔接到语义标注会话 |
| 17 | §5.2 | 删 `nanoMODBUS` / `modbus_port_openvela.c` / `TIOCSRS485` / `tcdrain()`；RS485 时序问题改为产品级表述（「半双工方向切换须由驱动层保证帧尾完整」） |
| 18 | §5.3 | 保留；用途改为「告警与规则库的证据来源」 |
| 19 | §5.4 | 工具表改为**能力描述**而非函数签名（如「连续采样并给出变化率统计」）；Skill 改为 `modbus_register_semantics.md`；主动任务改为事件触发 |
| 20 | §5.5 | 整节替换为「主动语义标注会话」，取 `design.md` §2.7 |
| 21 | §5.7 | 保留 9 条决策表；地位提升为「诊断的唯一路径」 |
| 22 | §5.6 告警 | 并入 §16.6 中属产品语义的部分（多 active alarm、首页显示最高优先级、ack ≠ resolved、时间字段） |
| 23 | §6.2 | 「诊断工单」改为「语义标注确认」 |
| 24 | §6.4 | 改为语义标注确认页：推断语义、单位、量纲、建议阈值、**证据列表**、`confidence_basis`、验证过程回放；保留「不提供让 AI 直接修复的入口」 |
| 25 | §7 | 场景一扩展为含语义标注的完整接入流程；场景二重写为规则库诊断；场景三降级口径更新 |
| 26 | 新增章节 | 上位机，取 `design.md` §4，含三条边界 |
| 27 | §8.1 | 删 `vg_esp_bearer.c` / `vg_net_mgr.c` / `vg_net_policy.c` |
| 28 | §8.4 | 删 `frameworks/system/ota/` bootctl；保留「XIP 导致 OTA 必须重启后由片内程序烧写」这条因果 |
| 29 | §9 存储布局 | **整节移出**（已在第一步迁走），手册留一句指向 |
| 30 | §10 | 整章替换为 `design.md` §2.5 + §2.6；§10.2 补记 `rs485_fault_triage.md` 的移除理由 |
| 31 | §11.1 | 删 `tool_guard.c` 提法；沙箱的产品级安全论点保留 |
| 32 | §12 | 「Agent 探查未收敛」改为「语义标注未收敛 → `unresolved`」；LLM 不可用行更新 |
| 33 | §13 | Skill 改名；新增上位机；视探针结果新增 H7 SAI 驱动与 WM8994 板级支持两个上游 PR |
| 34 | §14.1 | 按新场景重写相关条目 |
| 35 | §14.2 | 整节替换为 `design.md` §2.8 |
| 36 | §14.3 | 整表替换为 `design.md` §6 |
| 37 | §15 | 不变（两个前置阻塞项仍然成立） |
| 38 | §16 | **整章移出**，留一句指向 `docs/velaguard-architecture-decisions.md` |

---

## 第三步：改写 `VelaGuard_推进方案.md`

| # | 章节 | 动作 |
|---|---|---|
| 1 | 头部 | 标 v3 |
| 2 | §2 新增 2.6「v2 → v3」 | 记录 485 故障归因被替换及五条理由；WM8994 事实更正；上位机的引入；手册解耦 |
| 3 | §2.3 | 语音行改为「重估中，待 SAI 探针」；删除错误的 WM8994 论据；「手机 Web 远程配置」行说明与上位机的区别（走 USB CDC，非远程） |
| 4 | §2.5 | 补一句：保留的 v1 决策已迁至 `docs/velaguard-architecture-decisions.md` |
| 5 | §4 | 新增 SAI + WM8994 探针（20 h 封顶），取 `design.md` §5，与 ai_agent 探针并列 |
| 6 | §5 阶段 0 | 基本不变 |
| 7 | §6 阶段 1 | 「阈值触发的主动诊断会话」替换为「事件触发的主动语义标注会话」；Skill 改名；**新增上位机 55–85 h**；新增语义验证工具三件套 |
| 8 | §7 阶段 2 | 规则库提升为诊断唯一路径；新增 B 场景；视探针结果新增完整语音 |
| 9 | §9 | 工时重估（上位机 55–85 h、探针 20 h）；**9/20 子集重排** |
| 10 | §10 | 重写最小可演示闭环：上电 → 扫描 → 语义标注 → 屏幕确认 → 采集 → 规则库告警 |
| 11 | §11 | 重写 5 分钟演示脚本，主线改为「陌生总线自动接入 + 语义化」，加上位机镜头 |
| 12 | §12 | 12.2 改为「语义标注准确率是否达标」；新增 SAI 探针决策点 |
| 13 | §13 | 不变 |

推进方案是**计划文档**，允许保留工时、风险、技术路径等开发向内容，不适用手册的解耦规则。

---

## 第四步：改 `CONTEXT.md` 术语表

- 新增 **Register Semantics（寄存器语义）**：一个寄存器所代表的物理量、单位、量纲与正常区间。_Avoid_：data type, word order
- 新增 **Semantic Annotation Session（语义标注会话）**：扫描完成后自动启动的 Agent 会话，用只读工具验证语义假设并产出候选点表
- 新增 **Host Configurator（上位机）**：经 USB CDC 连接的配置期工具，可创作配置但不可激活配置。_Avoid_：PC sidecar, remote console
- 复核 **Manual Profile** 是否仍然需要（手册解析已在 v2 删除，该术语可能已失效）
- 复核 **Candidate Configuration** 是否覆盖新的候选点表

---

## 第五步：修连带引用

- `docs/velaguard-mqtt-contract.md` 头部「依据 `VelaGuard_项目手册.md` §8.3–8.6」→ 改指向新的架构决策文档
- 全仓搜索 `项目手册 §16`、`手册 §9` 等交叉引用，逐一改指向
- `docs/agents/BOUNDARY.md:126` 的文档分层表新增一行，登记架构决策文档的角色

---

## 第六步：验证

文档任务无编译验证，改用人工核对：

**场景改写**

- [ ] 无残留的「诊断模式」「rs485_fault_triage」「Agent 自主探测实验」措辞
- [ ] 全文搜索 `WM8994`，「树内无驱动」表述已全部更正
- [ ] 全文搜索 `语音`，口径统一为「待 SAI 探针」
- [ ] §2.3 / §11.2 的安全边界一条未丢
- [ ] 上位机三条边界在手册中都能找到
- [ ] 对照 `BOUNDARY.md` 的 C9 / C11 / V1 / V5，新方案全部合规
- [ ] 新 §14.2 是「一个可能失败的数字」，且可离线复现

**解耦**

- [ ] 手册全文搜索：无 `.c` / `.h` 文件名、无函数签名、无 `CONFIG_` 选项名、无十六进制地址
- [ ] 逐条比对，§16 十节 + §9 存储布局在新文档中一条未丢
- [ ] §16.6 的产品语义部分确实留在了手册，未随技术部分一并迁走
- [ ] §3 的硬件约束**因果**仍在（尤其「XIP 导致 OTA 必须重启后烧写」）
- [ ] 通读手册，确认读起来像产品设计方案而非实现说明

---

## 回滚点

改动涉及手册、推进方案、`CONTEXT.md`、新建架构决策文档，以及若干引用修正。建议分四次提交，便于单独回退：

1. 新建架构决策文档 + §16/§9 迁移（纯搬运，diff 可逐行核对）
2. 手册改写（场景 + 解耦）
3. 推进方案改写
4. `CONTEXT.md` 与连带引用修正
